<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Dashboard</title>
    <!-- Include Bootstrap CSS here -->
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" type="text/css" href="/css/index.css" />
    <style>
        /* Center align buttons within a container */
        .button-container {
            display: flex;
            justify-content: center;
            margin-top: 20px;
        }
        button{
          color: aquamarine;
          font-size: 20px;
          cursor: pointer;
          outline: none;
          transition: all 0.3s ease;
          letter-spacing: 1px;
          padding: 10px 20px;
          border: none;
          border-radius: 5px;
          background-image: linear-gradient(-90deg,#ffa700 ,#febd00 );
          box-shadow: 0 5px 10px rgba(255, 167, 0, 0);
          transform: translateY(0px);
          opacity: 1;
          position: relative;
          overflow: hidden;
          z-index: 1;
        }

        button::before {
          content: "";
          position: absolute;
          top: -2px;
          left: -2px;
          height: calc(100% + 4px);
          width: calc(100% + 4px);
          background: linear-gradient(-90deg,rgba(255, 167, 0, 0) ,
          rgba(255, 255, 255, 0));
          transform: rotateZ(45deg);
          z-index: -1;
        }

        button::after {
          content: "";
          position: absolute;
          bottom: -2px;
          right: -2px;
          height: calc(100% + 4px);
          width: calc(100% + 4px);
          background: linear-gradient(90deg,rgba(255, 167, 0, 0),
          rgba(255, 255, 255, 0));
          transform: rotateZ(-45deg);
          z-index: -1;
        }

        button:hover {
          filter: brightness(80%);
          transform: translateY(-3px);
          box-shadow: 0 10px 20px rgba(255, 167, 0, .0);
        }

        button:active {
          transform: translateY(3px);
          box-shadow: 0 5px 10px rgba(255, 167, 0, .0);
        }
      
    </style>
</head>
<body>
    <header>
        <nav>
            <div class="logo">
                <strong><a href="/">Book Drive test</a></strong>
            </div>
            <ul>
                <li>
                  <a href="/logout">Logout</a>
                </li>
              </ul>
              
        </nav>
    </header>

    <section class="hero">
        <div class="hero-title">
            <h1>DASHBOARD</h1>
            <div>Driving test Booking</div>
        </div>
    </section>
    
    <div class="button-container">
        <% if (user.userType === 'Admin') { %>
            <!-- Display the Appointment button only for Admin -->
            <button class="btn" onclick="window.location.href='/appointment'">Appointments</button>
        <% } else if (user.userType === 'Driver'){ %>
            <button class="btn" onclick="window.location.href='/g'">G-test</button>
            <button class="btn" onclick="window.location.href='/g2'">G2-test</button>
        <% } %>

    </div>
    


    <script src="/js/jquery.min.js"></script>
    <script src="/js/bootstrap.min.js"></script>
</body>
</html>
